#include <stdio.h>

int main() {
    int num1, num2, sum;
    if (scanf("%d", &num1) != 1) {
        printf("Invalid input. Please enter an integer.\n");
        return 1; // Exit with error
    }

    printf("Enter second integer: ");
    if (scanf("%d", &num2) != 1) {
        printf("Invalid input. Please enter an integer.\n");
        return 1; // Exit with error
    }

